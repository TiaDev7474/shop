import {montserrat} from "@/app/font";

export default function Logo() {
    return(
        <div className="text-gray-800">
            <h2 className={`${montserrat.className} font-bold text-xl`}>.DiaryShop</h2>
        </div>
    )
}
